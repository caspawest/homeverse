{
  "name": "homeverse",
  "version": "1.0.0",
  "description": "Homeverse - Real estate website (HTML, CSS, JavaScript)",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "collectCoverageFrom": [
      "assets/js/**/*.js"
    ],
    "coverageDirectory": "coverage"
  }
}
